node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
coverage

# TanStack Router generated tree (regenerated by the plugin/CLI)
# Keep it out of source control to avoid churn; the dev server regenerates it.
src/routeTree.gen.ts
