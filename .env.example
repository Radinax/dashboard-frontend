# Base URL for the dashboard-backend API.
# In dev, leave this as "/api" and let Vite proxy it to the backend (see vite.config.ts),
# so the browser stays same-origin and the httpOnly refresh cookie flows.
# In production, point it at the deployed API origin (e.g. https://api.example.com/api).
VITE_API_URL=/api
